<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="content">

    <!-- for writing stories to external storage -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/bear"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/bear"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DrawWriteActivity" android:screenOrientation="landscape"></activity>
        <activity android:name=".ChooseStoryActivity" android:screenOrientation="landscape"></activity>
        <activity android:name=".ReadStoryActivity" android:screenOrientation="landscape"></activity>

    </application>

</manifest>